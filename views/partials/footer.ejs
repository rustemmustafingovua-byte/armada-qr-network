    </main>
  </div>
</div>
<noscript>
  <div class="bg-amber-50 dark:bg-amber-500/10 border-t border-amber-200 dark:border-amber-500/20 text-amber-700 dark:text-amber-400 text-center p-3 text-sm">
    <div class="flex items-center justify-center gap-2">
      <i data-lucide="alert-triangle" class="w-4 h-4"></i>
      JavaScript is disabled — some features won't work.
    </div>
  </div>
</noscript>
<script>
(function(){
  var csrf = window.__csrf || '';
  document.querySelectorAll('form[method="POST"]').forEach(function(f) {
    if (!f.querySelector('input[name="_csrf"]')) {
      var inp = document.createElement('input');
      inp.type = 'hidden'; inp.name = '_csrf'; inp.value = csrf;
      f.prepend(inp);
    }
  });
  if (document.querySelector('canvas') && !window.Chart) {
    var s = document.createElement('script');
    s.src = 'https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js';
    s.defer = true;
    s.crossOrigin = 'anonymous';
    document.head.appendChild(s);
  }
})();
</script>
<script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
</body>
</html>
