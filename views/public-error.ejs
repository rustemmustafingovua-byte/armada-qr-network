<%- include('partials/header') %>
<div class="max-w-md mx-auto mt-8 sm:mt-16 px-4 fade-in">
  <div class="bg-white dark:bg-gray-800 p-8 rounded-xl shadow-md dark:shadow-2xl border dark:border-gray-700 text-center">
    <div class="text-5xl mb-4">⚠️</div>
    <h1 class="text-xl font-bold mb-2 dark:text-white">Error <%= typeof code !== 'undefined' ? code : '' %></h1>
    <p class="text-gray-500 dark:text-gray-400 text-base"><%= typeof message !== 'undefined' ? message : 'Something went wrong' %></p>
  </div>
</div>
<%- include('partials/footer') %>
