<%- include('partials/header') %>
<div class="max-w-md mx-auto mt-8 sm:mt-16 px-4">
  <div class="bg-white p-8 rounded-xl shadow-md border text-center">
    <div class="text-5xl mb-4">⚠️</div>
    <h1 class="text-xl font-bold mb-2">Error <%= typeof code !== 'undefined' ? code : '' %></h1>
    <p class="text-gray-500 text-base"><%= typeof message !== 'undefined' ? message : 'Something went wrong.' %></p>
  </div>
</div>
<%- include('partials/footer') %>
